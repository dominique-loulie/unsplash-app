// Import the required module from React !!
import React from 'react';

// Define a functional component named Home !!
function Home() { 

  return (
    // Create a div with class name "home" !!
    <div className="home">

      {/* Display a sub-title */}
      <h2>Welcome to Unsplash</h2>

      {/* Display a description */}
      <p>Explore beautiful, free images contributed by photographers from around the world. Discover high-quality visuals that are better than stock photos.</p>
    </div>
  );
}

// Export the Home component as the default export of the module !!
export default Home;

