// Import the required module from React !!
import React from 'react';

// Define a functional component named Head !!
const Head = () => {
  return (

    // Create a header section
    <header>

      {/* Display the main title */}
      <h1>Image <br/>Search<br/>App.</h1>
      
      {/* Display the attribution link */}
      <p>By <a href="https://unsplash.com/">Unsplash</a>.</p>
    </header>
  );
}

// Export the Head component as the default export of the module !!
export default Head;

